[tool.poetry]
name = "honcho-sdk"
version = "0.1.0"
description = "Python Client SDK for Honcho"
authors = ["Vineeth Voruganti"]
license = "AGPL-3.0"
readme = "README.md"
packages = [{include = "honcho_sdk"}]

[tool.poetry.dependencies]
python = "^3.11"
python-dotenv = "^1.0.0"
requests = "^2.31.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
