#![allow(clippy::unwrap_used, clippy::expect_used, clippy::panic, missing_docs)]

#[test]
fn crate_compiles() {
    // TODO: add real smoke test (e.g. create workspace via honcho_types construction)
}
