{/*
  GENERATED by honcho-cli/scripts/generate_cli_docs.py — do not edit.
  Re-generate with: uv run --package honcho-cli python honcho-cli/scripts/generate_cli_docs.py
  Source of truth: honcho-cli/src/honcho_cli/commands/
*/}

## honcho conclusion

List, search, create, and delete peer conclusions (Honcho's memory atoms).

**Options**

- `--json` — Force JSON output
- `--workspace` / `-w` — Override workspace ID
- `--peer` / `-p` — Override peer ID
- `--session` / `-s` — Override session ID

**honcho conclusion create**

Create a conclusion.

**Arguments**

- `<content>`

**Options**

- `--observer` — Observer peer ID
- `--observed` — Observed peer ID
- `--session` / `-s` — Session context
- `--workspace` / `-w` — Override workspace ID
- `--peer` / `-p` — Override peer ID
- `--json` — Force JSON output

**honcho conclusion delete**

Delete a conclusion.

**Arguments**

- `<conclusion_id>`

**Options**

- `--observer` — Observer peer ID
- `--observed` — Observed peer ID
- `--yes` / `-y` — Skip confirmation
- `--workspace` / `-w` — Override workspace ID
- `--peer` / `-p` — Override peer ID
- `--json` — Force JSON output

**honcho conclusion list**

List conclusions.

**Options**

- `--observer` — Observer peer ID
- `--observed` — Observed peer ID
- `--limit` — Max results
- `--workspace` / `-w` — Override workspace ID
- `--peer` / `-p` — Override peer ID
- `--json` — Force JSON output

**honcho conclusion search**

Semantic search over conclusions.

**Arguments**

- `<query>`

**Options**

- `--observer` — Observer peer ID
- `--observed` — Observed peer ID
- `--top-k` — Max results
- `--workspace` / `-w` — Override workspace ID
- `--peer` / `-p` — Override peer ID
- `--json` — Force JSON output

## honcho config

Inspect CLI configuration.

**Options**

- `--json` — Force JSON output

## honcho doctor

Verify config and connectivity. Scope with -w / -p to check workspace, peer, and queue health.

**Options**

- `--json` — Force JSON output

## honcho help

Show help message.

## honcho init

Set API key and server URL in ~/.honcho/config.json.

Press Enter to keep the current value or type a replacement.
Workspace / peer / session scoping is per-command via -w / -p / -s
or HONCHO_* env vars — never persisted.

**Options**

- `--api-key` — API key (admin JWT)
- `--base-url` — Honcho API URL (e.g. https://api.honcho.dev, http://localhost:8000)
- `--json` — Force JSON output

## honcho message

List, create, and get messages within a session.

**Options**

- `--json` — Force JSON output
- `--workspace` / `-w` — Override workspace ID
- `--peer` / `-p` — Override peer ID
- `--session` / `-s` — Override session ID

**honcho message create**

Create a message in a session.

**Arguments**

- `<content>`

**Options**

- `--peer` / `-p` — Peer ID of the message sender
- `--metadata` — JSON metadata to associate with the message
- `--session` / `-s` — Session ID
- `--workspace` / `-w` — Override workspace ID
- `--json` — Force JSON output

**honcho message get**

Get a single message by ID.

**Arguments**

- `<message_id>`

**Options**

- `--session` / `-s` — Session ID
- `--workspace` / `-w` — Override workspace ID
- `--json` — Force JSON output

**honcho message list**

List messages in a session. Scoped to a peer with -p.

**Arguments**

- `<session_id>` (optional)

**Options**

- `--last` — Number of recent messages
- `--reverse` — Show oldest first (default is newest first)
- `--brief` — Show only IDs, peer, token count, and created_at (no content)
- `--workspace` / `-w` — Override workspace ID
- `--peer` / `-p` — Filter by peer ID
- `--session` / `-s` — Override session ID
- `--json` — Force JSON output

## honcho peer

List, create, chat with, search, and manage peers and their representations.

**Options**

- `--json` — Force JSON output
- `--workspace` / `-w` — Override workspace ID
- `--peer` / `-p` — Override peer ID
- `--session` / `-s` — Override session ID

**honcho peer card**

Get raw peer card content.

**Arguments**

- `<peer_id>` (optional)

**Options**

- `--target` — Target peer for relationship card
- `--workspace` / `-w` — Override workspace ID
- `--peer` / `-p` — Override peer ID
- `--json` — Force JSON output

**honcho peer chat**

Query the dialectic about a peer.

**Arguments**

- `<query>`

**Options**

- `--target` — Target peer for perspective
- `--reasoning` / `-r` — Reasoning level: minimal, low, medium, high, max
- `--workspace` / `-w` — Override workspace ID
- `--peer` / `-p` — Override peer ID
- `--session` / `-s` — Override session ID
- `--json` — Force JSON output

**honcho peer create**

Create or get a peer.

**Arguments**

- `<peer_id>`

**Options**

- `--observe-me` / `--no-observe-me` — Whether Honcho will form a representation of this peer
- `--metadata` — JSON metadata to associate with the peer
- `--workspace` / `-w` — Override workspace ID
- `--json` — Force JSON output

**honcho peer get-metadata**

Get metadata for a peer.

**Arguments**

- `<peer_id>` (optional)

**Options**

- `--workspace` / `-w` — Override workspace ID
- `--peer` / `-p` — Override peer ID
- `--json` — Force JSON output

**honcho peer inspect**

Inspect a peer: card, session count, recent conclusions.

**Arguments**

- `<peer_id>` (optional)

**Options**

- `--workspace` / `-w` — Override workspace ID
- `--peer` / `-p` — Override peer ID
- `--json` — Force JSON output

**honcho peer list**

List all peers in the workspace.

**Options**

- `--workspace` / `-w` — Override workspace ID
- `--json` — Force JSON output

**honcho peer representation**

Get the formatted representation for a peer.

**Arguments**

- `<peer_id>` (optional)

**Options**

- `--target` — Target peer to get representation about
- `--search-query` — Semantic search query to filter conclusions
- `--max-conclusions` — Maximum number of conclusions to include
- `--workspace` / `-w` — Override workspace ID
- `--peer` / `-p` — Override peer ID
- `--session` / `-s` — Override session ID
- `--json` — Force JSON output

**honcho peer search**

Search a peer's messages.

**Arguments**

- `<query>`

**Options**

- `--limit` — Max results
- `--workspace` / `-w` — Override workspace ID
- `--peer` / `-p` — Override peer ID
- `--json` — Force JSON output

**honcho peer set-metadata**

Set metadata for a peer.

**Arguments**

- `<metadata>`

**Options**

- `--workspace` / `-w` — Override workspace ID
- `--peer` / `-p` — Peer ID (uses default if omitted)
- `--json` — Force JSON output

## honcho session

List, inspect, create, delete, and manage conversation sessions and their peers.

**Options**

- `--json` — Force JSON output
- `--workspace` / `-w` — Override workspace ID
- `--peer` / `-p` — Override peer ID
- `--session` / `-s` — Override session ID

**honcho session add-peers**

Add peers to a session.

**Arguments**

- `<session_id>`
- `<peer_ids>`

**Options**

- `--workspace` / `-w` — Override workspace ID
- `--json` — Force JSON output

**honcho session context**

Get session context (what an agent would see).

**Arguments**

- `<session_id>` (optional)

**Options**

- `--tokens` — Token budget
- `--summary` / `--no-summary` — Include summary
- `--workspace` / `-w` — Override workspace ID
- `--session` / `-s` — Override session ID
- `--json` — Force JSON output

**honcho session create**

Create or get a session.

**Arguments**

- `<session_id>`

**Options**

- `--peers` — Comma-separated peer IDs to add to the session
- `--metadata` — JSON metadata to associate with the session
- `--workspace` / `-w` — Override workspace ID
- `--json` — Force JSON output

**honcho session delete**

Delete a session and all its data. Destructive — requires --yes or interactive confirm.

**Arguments**

- `<session_id>` (optional)

**Options**

- `--yes` / `-y` — Skip confirmation
- `--workspace` / `-w` — Override workspace ID
- `--session` / `-s` — Override session ID
- `--json` — Force JSON output

**honcho session get-metadata**

Get metadata for a session.

**Arguments**

- `<session_id>` (optional)

**Options**

- `--workspace` / `-w` — Override workspace ID
- `--session` / `-s` — Override session ID
- `--json` — Force JSON output

**honcho session inspect**

Inspect a session: peers, message count, summaries, config.

**Arguments**

- `<session_id>` (optional)

**Options**

- `--workspace` / `-w` — Override workspace ID
- `--session` / `-s` — Override session ID
- `--json` — Force JSON output

**honcho session list**

List sessions in the workspace.

**Options**

- `--peer` / `-p` — Filter by peer
- `--workspace` / `-w` — Override workspace ID
- `--json` — Force JSON output

**honcho session peers**

List peers in a session.

**Arguments**

- `<session_id>` (optional)

**Options**

- `--workspace` / `-w` — Override workspace ID
- `--session` / `-s` — Override session ID
- `--json` — Force JSON output

**honcho session remove-peers**

Remove peers from a session.

**Arguments**

- `<session_id>`
- `<peer_ids>`

**Options**

- `--workspace` / `-w` — Override workspace ID
- `--json` — Force JSON output

**honcho session representation**

Get the representation of a peer within a session.

**Arguments**

- `<peer_id>`
- `<session_id>` (optional)

**Options**

- `--target` — Target peer (what peer_id knows about target)
- `--search-query` — Semantic search query to filter conclusions
- `--max-conclusions` — Maximum number of conclusions to include
- `--workspace` / `-w` — Override workspace ID
- `--session` / `-s` — Override session ID
- `--json` — Force JSON output

**honcho session search**

Search messages in a session.

**Arguments**

- `<query>`
- `<session_id>` (optional)

**Options**

- `--limit` — Max results
- `--workspace` / `-w` — Override workspace ID
- `--session` / `-s` — Override session ID
- `--json` — Force JSON output

**honcho session set-metadata**

Set metadata for a session.

**Arguments**

- `<session_id>` (optional)

**Options**

- `--data` / `-d` — JSON metadata to set (e.g. '\{"key": "value"\}')
- `--workspace` / `-w` — Override workspace ID
- `--session` / `-s` — Override session ID
- `--json` — Force JSON output

**honcho session summaries**

Get session summaries (short + long).

**Arguments**

- `<session_id>` (optional)

**Options**

- `--workspace` / `-w` — Override workspace ID
- `--session` / `-s` — Override session ID
- `--json` — Force JSON output

## honcho workspace

List, create, inspect, delete, and search workspaces.

**Options**

- `--json` — Force JSON output
- `--workspace` / `-w` — Override workspace ID
- `--peer` / `-p` — Override peer ID
- `--session` / `-s` — Override session ID

**honcho workspace create**

Create or get a workspace.

**Arguments**

- `<workspace_id>`

**Options**

- `--metadata` — JSON metadata to associate with the workspace
- `--json` — Force JSON output

**honcho workspace delete**

Delete a workspace. Use --dry-run first to see what will be deleted.

Requires --yes to skip confirmation, or will prompt interactively.
If sessions exist, requires --cascade to delete them first.

**Arguments**

- `<workspace_id>`

**Options**

- `--yes` / `-y` — Skip confirmation prompt (for scripted/agent use)
- `--cascade` — Delete all sessions before deleting the workspace
- `--dry-run` — Show what would be deleted without deleting
- `--json` — Force JSON output

**honcho workspace inspect**

Inspect a workspace: peers, sessions, config.

**Arguments**

- `<workspace_id>` (optional)

**Options**

- `--workspace` / `-w` — Override workspace ID
- `--json` — Force JSON output

**honcho workspace list**

List all accessible workspaces.

**Options**

- `--json` — Force JSON output

**honcho workspace queue-status**

Get queue processing status.

**Options**

- `--workspace` / `-w` — Override workspace ID
- `--session` / `-s` — Override session ID
- `--observer` — Filter by observer peer
- `--sender` — Filter by sender peer
- `--json` — Force JSON output

**honcho workspace search**

Search messages across workspace.

**Arguments**

- `<query>`

**Options**

- `--workspace` / `-w` — Override workspace ID
- `--limit` — Max results
- `--json` — Force JSON output
