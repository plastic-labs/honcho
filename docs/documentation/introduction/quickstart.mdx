---
title: 'Quickstart'
description: 'Start building with Honcho in under 5 minutes'
icon: 'bolt'
---


<Note>
This uses the demo server at https://demo.honcho.dev
</Note>

Install the Honcho client SDK with the following commands:

<CodeGroup>
```bash Python (uv)
uv add honcho-ai
```

```bash Python (pip)
pip install honcho-ai
```

```bash TypeScript (npm)
npm install @honcho-ai/sdk
```

```bash TypeScript (yarn)
yarn add @honcho-ai/sdk
```

```bash TypeScript (pnpm)
pnpm add @honcho-ai/sdk
```
</CodeGroup>

First, import the honcho client from the package:

<CodeGroup>
```python Python
from honcho import Honcho
# Initialize client (using the default workspace and demo environment)
client = Honcho()
```

```javascript NodeJS
import { Honcho } from '@honcho-ai/sdk';
// Initialize client (using the default workspace and demo environment)
const client = new Honcho();
```
</CodeGroup>

Next, we want to create peers within our honcho client:

<CodeGroup>

```python Python
alice = client.peer("alice")
bob = client.peer("bob")
```

```javascript NodeJS
const alice = client.peer("alice")
const bob = client.peer("bob")
```

</CodeGroup>


Create a session and add our peers to it:

<CodeGroup>

```python Python
session = client.session("session_1")
session.add_peers([alice, bob])
```

```javascript NodeJS
const session = client.session("session_1")
session.addPeers([alice, bob])
```

</CodeGroup>

Add peer messages to the session:

<CodeGroup>

```python Python
session.add_messages(
    [
        alice.message("Hi Bob, how are you?"),
        bob.message("I'm good, thank you!"),
        alice.message("What are you doing today after work?"),
        bob.message("I'm going to the gym!"),
    ]
)
```

```javascript NodeJS
session.addMessages([
    alice.message("Hi Bob, how are you?"),
    bob.message("I'm good, thank you!"),
    alice.message("What are you doing today after work?"),
    bob.message("I'm going to the gym!"),
])
```

</CodeGroup>

You can then chat with honcho to get a dialectic response:

<CodeGroup>

```python Python
response = alice.chat("What is Bob doing after work?")
```

```javascript NodeJS
const response = await alice.chat("What is Bob doing after work?");
```

</CodeGroup>


This is a super simple overview of how to get up and running with the Honcho SDK. We covered the basic methods for creating peers, sessions, and messages, as well as retrieving basic insights.
For a more detailed look at the SDK, check out the SDK reference [here](../platform/sdk).
